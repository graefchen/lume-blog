---
layout: layouts/base.vto
---

<article>
	{{ if thumbnail }}
		<img src="{{ thumbnail }}">
	{{ /if }}
	<h1>{{ title }}</h1>
	<p>
		<time datetime="{{ date |> date('DATETIME') }}">
		{{ date |> date('HUMAN_DATE') }}
		</time>
	</p>
	<p>{{ excerpt |> md }}</p>
	{{ content }}
</article>